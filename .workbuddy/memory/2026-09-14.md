# 2026-09-14

## README 冗余清理（中英文同步）
- 用户感觉 README 有重复但说不上来。评估后定位主冗余：「Why Fork → All Retained Core Features／保留的全部基础功能」整节几乎被「Features／功能特性」原样复述。
- 处理（README.md + README_zh-CN.md 同步）：
  1. 删除 "### All Retained Core Features／保留的全部基础功能" 整节（Why Fork 现仅留 Security Upgrades + Architecture Simplification 两个差异化子节）。
  2. 抢救该节独有、Features 原本没有的两点并入 Features：**Mobile support／移动端支持**、**Empty folder cleanup／空文件夹清理**。
  3. "Auto Sync／自动同步" 节首句与 Features 的 Auto sync 条目重复 → 删首句，仅保留两条独有限制（静默失败 / 关闭 Obsidian 后不运行）。
  4. 开头 "Pull requests are welcome／欢迎提交 Pull Request！" 与结尾 Feedback 重复 → 删开头句。
- 验证：两版 README 结构对称、无残留重复；移动端表格溢出问题此前已修（无 `|` 表格）。

## README 章节层级修正（用户确认执行）
- 评估整体段落章节后，定位层级不一致：`## Auto Sync／自动同步` 与 `## Hidden Files／隐藏文件` 是顶级标题，但属「使用」操作细则，与 `Usage／使用` 下的 `### S3/WebDAV/OneDrive` 不同级。
- 处理：将上述两者由 `##` 降为 `###`，归到 `Usage／使用` 下（两份 README 同步）。现大纲层级一致：Usage 含 5 个 ### 子节（S3/WebDAV/OneDrive/Auto Sync/Hidden Files）。
- 未做：TOC 目录、intro 拆分（用户未要求，属可选优化）。
